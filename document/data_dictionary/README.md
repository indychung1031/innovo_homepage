# Data Dictionary

Innovo_homepage PostgreSQL 스키마 문서를 관리합니다.

## 파일 규칙

- `00_schema.md` — 전체 테이블·컬럼 정의
- DB 스키마 변경(Alembic revision) 시 반드시 본 폴더 문서를 함께 업데이트한다.

## DB 정보

- Engine: PostgreSQL
- Database: `innovo_homepage` (`.env`의 `POSTGRES_DB`)
